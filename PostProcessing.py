import numpy as np
import pandas as pd

def abstention_fit(x_test,y_prob, low = 0, high = 0.16, step = 0.01, error = 'symmetric'):
    if error is 'symmetric':
        return 

    else:
        return